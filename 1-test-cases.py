import sys
import math
import string
# Read number of test cases from stdin.
Test_line = sys.stdin.readline().rstrip() # Default is a STRING

T = int(Test_line)
print("number of test cases:", T)

for i in range(T):
    # Read the next line from standard input.
    # Each test case is assumed to contain two integers seperated by whotespace.

     # Split the line into separate string tokens.

    # Convert string tokens into integers using map().
    # map() is lazy, meaning conversion happens during iteration.

    # Compute the required outputs.
    # compute the total of both numbers added together
    # compute the product of both numbers

    # print result
    T = sys.stdin.readline().rstrip()
    
   
